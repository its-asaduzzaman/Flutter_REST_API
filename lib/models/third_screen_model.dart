class ThirdScreenModel {
  String name, email, body;
  int postId, id;
  ThirdScreenModel({
    required this.body,
    required this.email,
    required this.name,
    required this.id,
    required this.postId,
  });
}
