class SecondScreenModel {
  String title, url, thumbnailUrl;
  int albumId, id;
  SecondScreenModel(
      {required this.title,
      required this.thumbnailUrl,
      required this.url,
      required this.albumId,
      required this.id});
}
